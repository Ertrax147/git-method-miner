services:
  redis:
    image: redis:alpine
    container_name: redis_broker

  miner:
    build: ./miner
    depends_on:
      - redis
    env_file: .env

  visualizer:
    build: ./visualizer
    depends_on:
      - redis
      
